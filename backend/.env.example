PORT=4000

# Supabase (projet "econoschool" existant) - clé service_role (SECRÈTE, jamais côté frontend)
SUPABASE_URL=https://xwsvlscuetopepdiclir.supabase.co
SUPABASE_SERVICE_KEY=

# Secret pour signer les tokens de session (mets une longue chaîne aléatoire)
JWT_SECRET=

# Les comptes Fondateur / Économe ne sont plus ici : ils se créent depuis
# l'application (page "Création de compte"), stockés dans la table `utilisateurs`
# de Supabase avec mot de passe hashé (bcrypt).
